# Optional LLM mode (runs fully offline without it).
# Copy to `.env` and set one provider; without it the example uses honest
# deterministic fallbacks (§59).

# simplest: OpenRouter (multi-vendor)
LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-chat
