# Local keys for the research demo (loaded via python-dotenv).
# Copy into .env and fill in; .env is in .gitignore.

# --- chat: OpenRouter · DeepSeek (fact extraction / answer assembly) ---
OPENROUTER_API_KEY=sk-or-v1-...
OPENROUTER_MODEL=deepseek/deepseek-v4-flash
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# --- chat: OpenAI-compatible generic provider (llm_from_env) ---
# OPENAI_BASE_URL=http://localhost:11434/v1
# OPENAI_API_KEY=...
# OPENAI_MODEL=qwen2.5:7b

# Optional live-web evidence pages (comma-separated), instead of the default
# Wikipedia fixtures:
# RESEARCH_URLS=https://en.wikipedia.org/wiki/Vitamin_D,https://en.wikipedia.org/wiki/Deep_learning